# Copy to .env and set real values.

# Signs session cookies and CSRF tokens. Generate: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change-me

# Signs the audit ledger. MUST stay stable across restarts or past entries stop
# verifying. Keep it separate from SECRET_KEY in production.
AUDIT_KEY=change-me-too
